---
name: codex-speed-start
description: Start a Codex task with speed-first gate workflow. Use when beginning a new task and needing task file/worklog creation plus initial guard checks.
---

# Codex Speed Start

Run:

```powershell
.\scripts\skill-codex.ps1 -Stage start -TaskName "<task name>" -Pack start
```

Rule:

- Run this before implementation.
