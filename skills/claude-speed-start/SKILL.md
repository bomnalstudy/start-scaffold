---
name: claude-speed-start
description: Start a Claude task with speed-first gate workflow. Use when beginning a new task and needing task file/worklog creation plus initial guard checks.
---

# Claude Speed Start

Run:

```powershell
.\scripts\skill-claude.ps1 -Stage start -TaskName "<task name>" -Pack start
```

Rule:

- Run this before implementation.
